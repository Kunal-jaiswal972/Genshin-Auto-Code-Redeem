services:
  redeemer:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    env_file:
      - ../.env
    volumes:
      - redeemer-data:/data
    restart: unless-stopped

volumes:
  redeemer-data:
