services:
  redeemer:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    env_file:
      - ../.env
    shm_size: "1gb"
    volumes:
      - redeemer-data:/data
    restart: unless-stopped

volumes:
  redeemer-data:
